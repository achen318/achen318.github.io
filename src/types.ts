export interface SocialType {
  name: string;
  url: string;
  icon: string;
}

export interface SkillType {
  name: string;
  icon: string;
  year: number;
}

export interface ProjectType {
  name: string;
  description: string;
  github: string;
}
